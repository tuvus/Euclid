#include "unit_card.h"
#include "game_scene.h"

void Play_Card(Entity entity, Card_Player* card_player, Vector2 pos) {
    // Card::Play_Card(card_player, pos);
    // for (int i = 0; i < unit_count; i++) {
    // auto components =
    // vector{&Transform_Component::component_type, &Unit_Component::component_type};
    // auto entity = game_scene.ecs->Create_Entity(new Entity_Type(components));
    // Init_Unit(game_scene.ecs, entity, game_scene.Get_Team_Path(card_player->team), 1, i * 10,
    // card_player->team, .4f, Game_Scene::Get_Team_Color(card_player->team));
    // }
}
